{
  "name": "node-flux-tools",
  "version": "1.0.0",
  "description": "FLUX node tools - halving schedule and node monitor",
  "main": "server.js",
  "scripts": {
    "start": "npm install && node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "http-proxy-middleware": "^3.0.0"
  }
}
